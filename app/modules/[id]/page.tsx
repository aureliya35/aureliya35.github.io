import Link from 'next/link';
import { modules } from '../../../lib/modules';
export default function ModulePage({params}:{params:{id:string}}){const m=modules.find(x=>x.id===params.id);return <main className="shell"><nav className="nav"><Link className="brand" href="/modules">← Modules</Link><Link className="pill" href="/">Home</Link></nav>{m?<section className="hero"><span className="badge">{m.category} • {m.status}</span><h1>{m.name}</h1><p className="lead">{m.description}</p><div className="grid"><div className="card"><h3>Core workflow</h3><p className="muted">This module is represented in the platform registry and can be expanded with forms, API routes, database tables, and automations.</p></div><div className="card"><h3>Realistic implementation</h3><p className="muted">No fictional services are assumed. Integrations should be added through real APIs, environment variables, and user permissions.</p></div></div></section>:<section className="hero"><h1>Module not found</h1></section>}</main>}
