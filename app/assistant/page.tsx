'use client';
import Link from 'next/link';import {useState} from 'react';
export default function Assistant(){const [input,setInput]=useState('');const [reply,setReply]=useState('');async function ask(){const res=await fetch('/api/assistant',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:input})});const data=await res.json();setReply(data.reply)}return <main className="shell"><nav className="nav"><Link className="brand" href="/">Vercel by Auréliya</Link><Link className="pill" href="/modules">Modules</Link></nav><section className="hero"><h1>AI Assistant</h1><p className="lead">Draft, organize, summarize, and plan inside your private Auréliya workspace.</p><textarea className="textarea" value={input} onChange={e=>setInput(e.target.value)} placeholder="Ask Vercel to help..."/><br/><br/><button className="button" onClick={ask}>Send</button>{reply&&<div className="card"><h3>Response</h3><p className="muted">{reply}</p></div>}</section></main>}
